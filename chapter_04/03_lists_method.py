l1 = [1,2,3,4,17,12]
# print(len(l1))

# l1.sort() sort the list
# l1.reverse()

# l1.append(3456) # adds the ends of the list
# l1.insert(3 , 34) # (index , value)

# pop() & remove() function remove the value from lists pop(index) remove(value)
l1.pop(1)
l1.remove(12)
print(l1)